git init is used to initialize a new Git repository in your project folder.
git init
Git creates a hidden .git folder inside the directory.
Your folder becomes a Git repository.
You can now track files and commits.
Initialized empty Git repository in /path/to/project/.git/
